<%= form_for(@licor) do |f| %>
  <% if @licor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@licor.errors.count, "error") %> prohibited this licor from being saved:</h2>

      <ul>
      <% @licor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
 
  <div class="field">
    <%= f.label :mililitros %><br>
    <%= f.text_field :mililitros %>
  </div>
  <div class="field">
    <%= f.label :valor %><br>
    <%= f.text_field :valor %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="field">
    <%= f.label :category_licor %>
    <%= f.collection_select :category_licor_id, CategoryLicor.all, :id, :nombre, class: "form-control", prompt: "Seleccione Categoría" %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
